# Sentry Configuration
EXPO_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ORG=your-sentry-organization
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# API Configuration
EXPO_PUBLIC_API_URL=https://api.example.com
EXPO_PUBLIC_DEFAULT_LOCALE=en

# MMKV Encryption
# Generate a secure random key for MMKV encryption (minimum 32 characters)
# Use a cryptographically secure random generator like:
# - Node.js: require('crypto').randomBytes(32).toString('hex')
# - Online: https://www.uuidgenerator.net/dev-tools/randoms (use 64 char hex)
# - Command line: openssl rand -hex 32
# Keep this key secure and consistent across app versions
EXPO_PUBLIC_AUTH_STORAGE_ENCRYPTION_KEY=your-secure-random-64-character-hex-key-here-replace-with-actual-key